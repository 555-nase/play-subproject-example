Play App with Subproject
=====================================

In Folder modules is an second Play App (TestApp), which can run normaly as Play App. When the HostApp runs, 
the Subproject can reach http://localhost:9000/test/... (for example http://localhost:9000/test/testapp1).
